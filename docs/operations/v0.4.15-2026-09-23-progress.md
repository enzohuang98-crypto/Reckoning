# v0.4.15 接續驗收：2026-09-23 Windows 實測

狀態：進行中。source runner、離線測試、已安裝 App、候選 binary 分開判定；此時尚未符合候選發布或 Latest promotion 門檻。

## 起點

| 項目 | 本輪核對 |
| --- | --- |
| Repository / main | `enzohuang98-crypto/Reckoning`；`ce03ba0685eaf8b499241ba3153db722eb0f2eae` |
| 工作來源 | PR #76 `fix/ai-explanation-completeness`；接手 head `43e63a716cf9823f09754a9f67b32aab917f122a`，本輪改動尚待新 commit 與 CI |
| Tags / Release | `v0.4.14` tag 指向舊 `0553f1e`，無同名 Release；Latest 為 `v0.4.13`；開始時 `v0.4.15` 尚未使用。建立新 tag 前重查。 |
| 已安裝 App | `C:\Program Files\xiangqi-analyzer\象棋AI分析講解.exe`，ProductVersion `0.4.13.0`；這不是本輪候選版。 |
| SecretStore 安全 metadata | Electron main process 正式 `SecretStore.getStatus()`：加密可用，OpenRouter 已配置，選定 `nvidia/nemotron-3-super-120b-a12b:free`。沒有輸出 key 或密文。 |

沒有手改 AppData 或已安裝程式。獨立 main worktree 只用於修改前重現。正式 AppData 的舊內建 Pikafish profile 指向已不存在的 per-user 安裝路徑，實際引擎位於 Program Files 安裝的 `resources\engine\pikafish.exe`；本輪加入只修復舊內建 profile 的搬遷處理，保留自訂引擎。

## 截斷診斷與真實固定案例

正式一鍵比較在 `initial_combined` 階段同時產生審查 JSON 與五段正文，送出 `max_tokens=4000`，單次 soft timeout 100 秒；重試、輸出 token 與全輪期限共用既有上限。原先只有精確 Ultra 免費模型 JSON 請求使用 `reasoning.max_tokens=1000`；Super 沒有控制。官方 Super 目錄將 reasoning 標為可選、預設啟用，endpoint 列出 `reasoning`、`reasoning_effort`、`response_format` 與 `max_tokens`；`exclude` 只是不回傳 reasoning 文字，不保證省去 reasoning token。來源：<https://openrouter.ai/nvidia/nemotron-3-super-120b-a12b:free/api>、<https://openrouter.ai/api/v1/models/nvidia/nemotron-3-super-120b-a12b:free/endpoints>、<https://openrouter.ai/docs/guides/best-practices/reasoning-tokens>。

以下均使用正式 SecretStore 已保存的同一把 key，僅在 Electron main process 解密；使用真實已安裝 Pikafish、`prepareExplanationExecution`、`buildAIExplanationRequest`、正式 Harness 與 validator。固定初始局面、`h2e2`「炮二平五」、理由「想用中炮控制中央並取得主動」、中性比較問題。它們是 source runner 證據，**不是已安裝 App UI 或候選 binary 驗收**。安全報告置於本機工作目錄上一層，未提交；只記模型、引擎主線、階段、預算、usage、結果和 validator 錯誤，不存 key、header、原始帳號回應或 hidden reasoning。

| 執行 | 結果 | 安全證據 |
| --- | --- | --- |
| main 修改前 | `initial_combined` 於約 43.5 秒以 `finish_reason=length`、completion 4000 結束；reasoning token **未提供**，未交付正文。 | `baseline-fixed-case.json` |
| PR 原策略 `reasoning.max_tokens=1000` | 一次 `stop`、completion 2681、reasoning 1006；正文 319 漢字與引用錯誤，被正式 validator 拒絕。 | `pr-fixed-case.json` |
| 改善主線引用提示後 | `length`、completion 4000、reasoning 3877；數值限制未被精確 Super endpoint 穩定遵守。 | `pr-fixed-case-second.json` |
| Super `effort=low` | `length`、completion 4000、reasoning 3822；low effort 仍占滿預算。 | `pr-fixed-case-low-effort.json` |
| Super `effort=none` | 第一次 provider 暫時失敗，既有有限重試成功：`stop`、completion 1497、reasoning 0；正文 190 漢字、審查與對手方別引用錯誤，validator 拒絕。 | `D:\DevTemp\reckoning-fixed-case-result.json` |
| Super `effort=none`，明示 JSON 範例不是正文 | 一次 `stop`、completion 1819、reasoning 0；正文 314 漢字，K1 具體詞與跨變例／對手方別引用仍錯，validator 拒絕。 | `pr-fixed-case-no-reasoning-revised-prompt.json` |

`effort=none` 的真實回應證明對此精確模型有效，解決本次 reasoning 吃滿輸出預算的截斷；仍缺完整合格答案。相同設定不再反覆呼叫。任何 `length`、空正文、錯誤模型身分、無效 JSON 與品質不合格均維持拒絕。

正式 `OpenRouterSavedModelService.switchModel` 另以相同已保存 key 嘗試官方免費、支援 JSON 輸出的 `google/gemma-4-31b-it:free`。兩次短探測均回報 `validation_failed`，第二次安全診斷確定 `stage=generation`、`category=rate_limited`、`retryAfterMs` 未提供；切換前後的 active model 均為 Super。限流範圍不明，沒有再試其他模型或繼續刷 API；這不是模型切換成功的 UI 證據。

## 程式修正與測試

PR 已有比較狀態（同首選、近似等值、有證據差異、證據不足）、正向五段標題、主線/方別/變例引用檢查、至少 400 漢字與短追問分流。本輪補上「不足」與實質錯誤同段時不得豁免、沒有使用者著法時審查不能強迫負面比較、失敗回應 token 計入共享預算、精確 Super reasoning 停用、Windows Electron main process 加入 Windows 系統 CA（保留 TLS 驗證）、舊內建引擎安裝路徑修復，以及候選 promotion workflow 對來源 run 與公開 installer jobs 的檢查。最終進度不得把引用或模型自填 verified 當成獨立棋理證明。

本輪已執行：`npm.cmd ci` 通過（0 個 npm 弱點）；`npm.cmd exec -- tsx --tsconfig tsconfig.node.json tests/unit/main/openRouterProvider.test.ts` 通過；`npm.cmd exec -- tsx --tsconfig tsconfig.node.json tests/integration/ai/harness.test.ts` 142/142；`npm.cmd run typecheck:node` 通過；`npm.cmd exec -- tsx --tsconfig tsconfig.node.json tests/e2e/engine.e2e.ts` 94/94；`tests/unit/main/engineRegistry.test.ts` 23/23。最終修改後的 `npm.cmd test` 通過（pretest 含 updater SDK；fake engine E2E 94/94）；`npm.cmd run build` 通過；`npm.cmd run security:audit` 通過（執行期 0 個 moderate 以上弱點、建置工具 0 個未處理弱點）；`npm.cmd run test:acceptance:ai` 離線 fixed=6、soak=358 通過；`npm.cmd run test:updater-sdk` 單獨通過。PR 先前 head 的 green CI 不能代替本輪最終 head。

本輪程式 commit `e926b91d9bda50d4dda3fe0300eeb7db756bc9c8` 已推送至 PR #76；Windows CI run `35802019183` 的 `Verify Windows desktop app` 全步驟成功，包括 typecheck、test、AI 離線 acceptance、security audit 與 build。這個綠燈仍不等於真實 provider、原生 UI 或候選 binary 驗收。

## 尚未通過的獨立門檻

- 真實 OpenRouter 尚未產生完整、通過 validator 且可獨立檢閱的 400+ 漢字五段正文；未取得正文 SHA-256 或通過結論。
- 原生設定頁同 key 切換、失敗保留舊模型、關閉重開與重啟持久化、實際分析頁固定案例尚未驗收。
- v0.4.15 候選 tag、Release、installer SHA-256、blockmap、metadata、NotSigned 與 SmartScreen 文案尚未建立或驗收。
- 候選 Windows 安裝更新、程序與捷徑版本、草稿保護、資料保留、隔離新版更新器背景下載與準備完成後 0 installer payload 均未實機驗收。原生 UI 工具目前不提供 Windows 原生 App 操作；需在完成其他獨立工作後集中人工確認。
- 本機 `Win32_OptionalFeature` 僅列出已安裝 VirtualMachinePlatform，未列出 Windows Sandbox/Hyper-V；`wsb.exe`、`vmrun`、`VirtualBoxVM` 未發現。需要提升權限才可用 `Get-WindowsOptionalFeature` 詳查，因此隔離 Windows client 尚無可用實機證據。
- PR 的本輪程式 head 與 CI 已完成，尚缺真實完整正文、候選 App 和隔離更新器驗收；無候選資產可 promotion，`/releases/latest` 仍為 v0.4.13。

原始碼保留 `forceCodeSigning: true`。本目標允許的候選/正式發布僅可走既有 workflow 的明確 unsigned-release 例外，Release 必須標示未簽章及 SmartScreen 限制；不得把未簽章說成已簽章。候選若需再改程式，使用下一個未使用 patch 版本，保留舊 tag 與資產。

## 接續診斷與修正（同日）

重新核對 origin/main `ce03ba0`、PR #76 head `7a1d939`、Latest v0.4.13、v0.4.15 未用，沒有改動其他人的工作。檢查 314 漢字那次失敗 trace 的實際 evidence：E1 是首選線 19 步，E2 是實戰步線但僅 2 步，後續 Pikafish 加深產生的 E3 才是實戰步的完整 25 步線。前一次提示硬寫 E2 為實戰步主線，與正式 validator 對同一變例及對手著法的要求衝突。修正為從當次 evidence 選足夠深的首選與實戰變例，範例引用同步使用所選 id；送給模型的證據包排除同引擎同著法的較淺舊線，trace 仍保留完整證據。原始首選線也太淺時，以同一局面、同一最佳著法的加深結果更新 E1；找不到兩條合格主線就明確失敗。

以 `npm.cmd exec -- tsx --tsconfig tsconfig.node.json tests/integration/ai/harness.test.ts` 先得到 142/143：E2 淺、E3 深的 prompt 測試紅燈；修正後 143/143。修改後 `npm.cmd test`、`npm.cmd run build`、`npm.cmd run security:audit`、`npm.cmd run test:acceptance:ai`（fixed=6、soak=358）與單獨 `npm.cmd run test:updater-sdk` 全部通過。

修正後只用同一已保存 Super 模型重試一次真實固定案例；安全報告 `pr-fixed-case-dynamic-evidence.json` 記錄真實 Pikafish depth 19、模型仍在 21 個合格免費模型目錄、`initial_combined` 兩次呼叫均為 503（約 0.65 秒、1.65 秒）。Harness 依既有上限停止，沒有正文或可供驗證的模型 token usage；這次只證明受控 503 分類與有限重試，**不能證明**新提示的真實正文品質。沒有因 503 增加第三次呼叫，也沒有切換至付費模型。

候選發布流程另查出公開 installer 的 Server proxy job 原本以 workflow dispatch 時的 `github.sha` 取測試腳本，可能在 tag commit 與 main HEAD 不同時錯用腳本或 package 版本。改成與候選建置、Release notes 同樣 checkout 精確 `inputs.tag`，並加靜態發布契約測試；測試先紅 78/79，修正後 79/79。此項仍須候選 workflow 實際執行才能算發布驗收。

## 更新器與資料保護複核

PR #76 最終程式 head `bea4aa73f85693327f19605fed5387af67af14c5` 的 Windows CI run `35803220255` 成功；PR 仍開啟且可合併。這僅完成程式檢查，不解除上列發布門檻。

原始碼複核：`AppUpdaterService` 發現新版後才依偏好背景準備；`autoDownload=false`、`autoInstallOnAppQuit=false`；下載後不自行關閉程式。`App` 的安裝入口在儲存前後檢查未提交草稿，`flushLatestSnapshot` 在保存期間資料又變動時重存最新快照。現有 renderer、main-process 與 electron-updater SDK 測試分別覆蓋草稿攔截、保存失敗、背景準備、單次下載、失敗重試、同版快取與快取毀損。SDK fixture 以假的 installer payload 測公開下載 API，無法證明正式 packaged App 的安裝、資料保留或重新啟動階段 0 installer payload。舊 `UpdatePolicy` 註解把背景準備誤寫成須先確認且下載後直接安裝，已修正註解；執行政策沒有改動。上述實機項目仍列為未通過。

## 真實生成再診斷與有界修補

同一已保存 Super 模型在服務恢復後回傳 `stop`、completion 1637、reasoning 0，正文僅 320 漢字並有跨變例引用；安全報告 `pr-fixed-case-after-recovery.json`。加入每條變例的精確對手應手與可複製著法後，下一次回傳 `stop`、completion 1564、reasoning 0，正文僅 276 漢字，審核 K2 仍不合格；安全報告 `pr-fixed-case-explicit-pv.json`。這些是真實 provider 失敗，不是驗收通過。固定案例的候選修補流程現在最多追加一次完整審核加正文修補，與首輪共用模型呼叫、輸出 token 和總時限；修補後仍須通過原 validator，沒有降低 400 漢字下限。離線 Harness 正反例 144/144 通過。

修補後一次真實固定案例：首輪 `stop`、completion 1612、reasoning 0，但正文 272 漢字且引用與 K1/K2 不合格；第二輪修補請求在 30 秒軟時限逾時，總共 2 次模型呼叫，無正文交付；安全報告 `pr-fixed-case-bounded-repair.json`。後續修正了修補逾時誤歸類為一般品質失敗的錯誤路徑，此分類修正尚無第二次真實逾時實測。

為避免把 Super 的品質問題誤認為帳戶所有免費模型問題，正式 `OpenRouterSavedModelService` 使用同一把 key 只探測一次目錄內、官方 endpoint 支援 structured outputs 且價格為 0 的 `qwen/qwen3.8-27b:free`；結果 `rate_limited`，`retryAfterMs` 未提供，active model 保持 Super。限流範圍不明，沒有繼續探測其他模型。官方能力資料：<https://openrouter.ai/qwen/qwen3.8-27b:free/api>、<https://openrouter.ai/api/v1/models/qwen/qwen3.8-27b:free/endpoints>。

這批修改後，本機 `npm.cmd test`、`npm.cmd run build`、`npm.cmd run security:audit`、`npm.cmd run test:acceptance:ai`（fixed=6、soak=358）及 `npm.cmd run test:updater-sdk` 均通過。仍沒有任何真實完整合格正文，因此 PR 不合併、候選版本與 Latest 均不發布。CI 必須以這批修改的最終 commit 重新執行。

## 補字路徑與服務錯誤複核

PR #76 `e20f903dfd8a03c468b545f125e792cd973271f0` 的 Windows CI run `35822605064` 全綠。再檢查發現既有 `completeGroundedShortInitialAnswer` 會把審核結論及固定一般原則接在不足 400 漢字的模型正文後，可能靠重述讓字數達標。這不符合正文不得靠重複／空話湊字的契約，因此移除該補字路徑：少於 400 漢字保持失敗；400–499 漢字而其他品質條件合格的正文保留原文，不為達到約 500–900 的目標硬填。測試改為檢查不足字數拒絕、最多一次有界修補及合格原文不被改寫。

首輪提示另移除與當局無關的術語清單和原始分數，把可見主線限於每條前 12 步。修正後一次真實固定案例安全報告 `pr-fixed-case-no-padding-focused-evidence.json`：Super 仍在免費目錄，首輪 `stop`、completion 1579、reasoning 0，正文僅 239 漢字，且 C4 提到引用變例不存在的著法並把單一主線寫成必然；修補呼叫回報 `provider_unavailable`，共兩次呼叫，未交付答案。修補階段原本將這類 provider 錯誤吞掉、最終歸為一般品質失敗；新增 503 正反測試後修正為保留原錯誤分類。這次實測發生在分類修正前，不能冒充修正後實測。

此輪修正後本機重跑 `npm.cmd test`、`npm.cmd run build`、`npm.cmd run security:audit`、`npm.cmd run test:acceptance:ai`、`npm.cmd run test:updater-sdk` 均通過；Harness 143/143，AI 離線 fixed=6、soak=358。修補 503 的回歸測試確認第二次呼叫失敗時不越過總呼叫上限，並保留 `provider_unavailable`。這些離線結果不等於真實 provider 的完整正文，也不等於安裝版 UI 或隔離更新環境驗收。

## 真實證據長度與修補策略複核

最終已推送 head `37da306e11059b0289bfa16c163ab39b2d777ee5` 的 Windows CI run `35823599117` 全綠。從上次固定案例隔離 trace 只提取安全的引擎摘要：E1 首選 19 步、原實戰 E2 2 步、加深後實戰 E3 25 步，`engineRounds=1`；摘要補入本機 `pr-fixed-case-no-padding-focused-evidence.json`。主線長度已足夠，不能把 239 漢字及跨變例引用歸因於引擎只提供兩步。

曾在未提交的本機實驗中嘗試：審核已通過時第二次只生成 answer JSON，並容許一次 503 重試後再用第三次呼叫修補；Harness 合成測試可過，但真實結果否定效益。安全報告 `pr-fixed-case-answer-only-repair.json` 記錄首呼叫 503、第二次 `stop`／completion 1850／reasoning 0，審核 K2 不合格，兩次呼叫上限內沒有修補機會。再以三次上限重試一次，`pr-fixed-case-repair-three-call-cap.json` 記錄首輪 `stop`／completion 1613／reasoning 0，正文 255 漢字；第二輪 answer-only `stop`／completion 1826／reasoning 0，正文仍只有 264 漢字並混用首選與實戰變例，正式 validator 拒絕，沒有第三次呼叫。此實驗沒有改善真實答案，相關程式與測試已撤回，未推送到 PR；不再用同一設定反覆刷 API。

SecretStore 在 Electron main process 只輸出安全比較結果：另存的 `nex-agi/nex-n2.5-pro:free` 不是 active Super 的同一把 key，不能當作本目標的同 key 驗收。正式 `OpenRouterSavedModelService.switchModel` 針對官方免費且精確 endpoint 支援 `response_format` 的 `google/gemma-4-26b-a4b-it:free` 做一次探測；結果 `rate_limited`、`retryAfterMs` 未提供，active model 保持 Super，未重新輸入或輸出 key。官方資料：<https://openrouter.ai/google/gemma-4-26b-a4b-it:free/api>、<https://openrouter.ai/api/v1/models/google/gemma-4-26b-a4b-it:free/endpoints>。連同先前 Gemma 31B、Qwen 27B 的 429，限流範圍仍記為 unknown，不能推論所有免費模型或帳戶都耗盡。

## 免費模型恢復後的同 key 切換與完整請求

隔數小時後，使用正式 `OpenRouterSavedModelService.switchModel` 對同一已保存 key 再探測一次 `google/gemma-4-26b-a4b-it:free`：短生成通過，原子切換成功；重新開啟 SecretStore 後仍是 Gemma，無須再次輸入 key。這只證明 main-process 短探測與持久化，尚未通過安裝版設定頁 UI。正式 Pikafish＋Harness 固定案例 `pr-fixed-case-gemma-26b.json` 在 `initial_combined` 以 HTTP 429 結束，無完整正文；在約一分鐘有界等待後僅重試一次，`pr-fixed-case-gemma-26b-after-wait.json` 仍於同階段 429，`retryAfterMs` 未提供。短探測成功不能代替 4,000-token JSON 請求成功。官方免費方案目前列出每日請求額度：<https://openrouter.ai/pricing/>；本次 429 的精確範圍與重置時間仍未知，不把它歸為確定的帳戶額度耗盡。

停止 Gemma 重試後，以同一正式切換服務把 active model 改回原本 Super，短探測成功；再開 SecretStore 確認 active model 為 `nvidia/nemotron-3-super-120b-a12b:free`。此操作未更換 key、未手改 AppData，也未動另存不同 key 的 Nex 憑證。實際 Windows 設定頁切換／重開及分析頁完整正文仍未驗收，PR 不合併、候選與 Latest 不發布。

## 候選 binary 原生 UI／隔離更新人工清單（尚未執行）

以下只在取得完整合格真實正文、合併後建立候選 Release，並核對 installer SHA-256 後執行；記錄畫面、版本、時間與結果，不輸入或拍攝 key。這份清單是待驗項目，不是通過證據。

1. 以候選 Release 的 installer 正常更新；在「設定 → 資料與系統」記錄版本，核對啟動程序路徑、桌面捷徑目標、棋局、設定、已保存 key 與 Pikafish。若 Windows 顯示未簽章／SmartScreen 或 UAC，記錄實際提示，由帳戶持有人完成必要確認。
2. 在「設定 → AI 與金鑰」保持 key 欄空白，點「讀取模型」，從「OpenRouter 免费模型」選另一個合格 `:free` 模型，點「使用此模型」；確認「目前使用中」更新。關閉再打開設定頁及重啟 App，選擇仍保留且不要求重新輸入 key。受控切換失敗時，原模型仍顯示為目前使用中。
3. 在實際分析頁提交初始局面 `h2e2`「炮二平五」與理由「想用中炮控制中央並取得主動」，核對完整五段正文、對應主線與無自相矛盾；截圖及去敏正文／SHA-256 須對應候選 binary。另檢查重試保留先前有效答案，舊局面的遲到回應不覆蓋新局面。
4. 在「設定 → 資料與系統 → 版本與自動更新」勾「在背景準備新版本」，確認準備期間可繼續使用且完成後 App 不自行退出；「立即檢查」／「立即背景準備」後狀態到已下載。未提交著法或理由時按「重新啟動完成更新」必須阻擋；保存途中新增草稿也須阻擋；清除草稿且成功保存後才可安裝。
5. 在隔離 Windows 環境以真正 packaged 前版與受控更新來源測新版更新器；不改正式來源或提前升 Latest。記錄背景下載、重開同版本快取、按「重新啟動完成更新」時 installer payload 傳輸量為 0（metadata 另計）、快取雜湊、退出／安裝／重啟與資料保留；另實測安裝失敗重試、過早 prepare、下載失敗及毀損快取恢復。若隔離環境不存在，逐項保持未通過，不以 SDK fixture 代替。

隔離環境只讀預檢：本機 `Get-Command Get-VM, New-VM, WindowsSandbox.exe, vmrun.exe, VBoxManage.exe` 均找不到；固定路徑的 Sandbox、VirtualBox 與 VMware 執行檔亦不存在。`docker.exe` 存在，但 `docker info` 無法連線至目前的 Linux engine daemon；它不是可直接執行 Windows packaged App 的隔離環境。`Get-WindowsOptionalFeature -Online -FeatureName Containers-DisposableClientVM` 回報需要提升權限，未改系統功能。故目前沒有已驗證可用的隔離 Windows 環境；第 7 節實機門檻保持未完成，候選版可用後仍需具備真正的隔離環境與必要的帳戶持有人 UAC 操作。

## 2026-09-26：限流恢復檢查與安全額度診斷

02:27 UTC 從 `fc5a78da56a67cc7ee6cd36aa77c7e970be261c6` 接續，tracked 工作目錄乾淨；PR #76 仍 OPEN / MERGEABLE，該 head 的 Windows CI `35853542265` SUCCESS。遠端 main 仍 `ce03ba0`；`v0.4.15` 無 tag，Latest 仍 v0.4.13；實際 Program Files 安裝的 ProductVersion 仍 0.4.13.0，不是候選 binary。

正式 `SecretStore` 在 Electron main process 讀取 active provider/model，仍為 OpenRouter Super；正式免費目錄包含 Gemma 26B 與 Qwen 27B。距前次限流三天後，執行 `electron.cmd tools/acceptance/local-switch-free-model.cjs google/gemma-4-26b-a4b-it:free` 一次，`validation_failed` / generation / `rate_limited`，`retryAfterMs` 未提供；before/after model 均為 Super。

官方 [Limits 文件](https://openrouter.ai/docs/api/reference/limits) 現已列出 `GET /api/v1/key` 的 `free_model_daily_requests` 安全計數。僅透過 main process 解密同一 saved key，以既有 `fetchAiResponseBounded` 與 `readJsonResponseBounded` 讀取 metadata，白名單輸出 HTTP status、active model 及三個非負整數；未輸出 key、header、label、帳戶原始回應、信用額度或思考內容。命令 `electron.cmd tools/acceptance/local-free-quota.cjs` 結果：HTTP 200，`used=0`、`limit=50`、`remaining=50`。這排除當下「每日免費請求計數已耗盡」，不證明 per-minute、DDoS 或 upstream 容量情況。

再核對 [精確 Qwen endpoint](https://openrouter.ai/api/v1/models/qwen/qwen3.8-27b:free/endpoints) 的零價格與 structured outputs/max_tokens 參數，以相同正式切換服务僅探測 Qwen 一次：同為 `validation_failed` / generation / `rate_limited`，未提供 retry-after，Super 保持原選擇。沒有完整生成或成功切換，不新增正文品質通過結論；`rateLimitScope` 仍 unknown，不再嘗試其他模型。這是新的限流證據，不能據此承諾單純等每日重置就會恢復；也沒有授權使用付費模型。

`Get-Command Get-VM,New-VM,WindowsSandbox.exe,vmrun.exe,VBoxManage.exe` 仍無結果。候選 Windows UI、實機安裝、隔離新版更新器仍未完成。這輪只改驗收文件，未修改程式或降低品質門檻；舊程式測試對應的 commit 保持 `fc5a78d`。本機額度診斷 script 是未提交的 acceptance 工具，不含密鑰，未納入 Release。

## 2026-09-26：原生 UI 工具恢復

提交 `66cfc6991adb9b618401c3183b80591fb1514992` 的 CI run `36211914646` 已 SUCCESS（02:32:21 UTC 完成）。新版 Computer Use skill `26.924.20706` 改用 `node_repl` 匯入 `@oai/sky`；初始化及 `sky.list_apps()` 成功，`com.xiangqi.analyzer` 為已安裝 App。正式 `sky.launch_app` 啟動後返回唯一視窗，process identifier 為 `C:\Program Files\xiangqi-analyzer\象棋AI分析講解.exe`；`sky.get_window_state` 成功取得分析頁 accessibility，棋盤為初始局面、Pikafish 正在主線搜尋。未送出 AI、未讀取 key、未改棋局或設定；工具能力確認後以 Alt+F4 正常關閉。

因此先前紀錄中的「原生 Windows UI 工具不可用」已不適用，後續候選 binary 可使用此正式原生操作介面；這次只確認舊安裝版 0.4.13 的工具能力，不是候選版設定頁、同 key 切換、完整講解或安裝驗收。隔離 Windows 環境與真實完整合格正文仍缺。

## 2026-09-26：修正版正式完整案例

程式 head `bfa5aaa557fece4c3d015360cc3847535fe8d05c` 的 CI run `36212147884` SUCCESS。在新日期的恢復檢查中，沒有再切換模型；只對原 active Super 做一次正式 Pikafish＋Harness 完整案例，命令 `RECKONING_SOURCE_COMMIT=bfa5aaa...`（PowerShell 環境變數）及 `electron.cmd tools/acceptance/local-openrouter-fixed-case.cjs ..\pr-fixed-case-super-2026-09-26.json`。這是 PR 修正版 source runner，不是 0.4.13 安裝版或候選 binary 驗收。

引擎 depth 18，最佳 `g3g4`，實戰 `h2e2`；Harness 全程 36,479 ms，兩次模型呼叫（initial_combined、repair），每次請求上限 4,000，reasoning_disabled，均回 `stop`，completion 1,690 / 1,825，reasoning 0 / 0。沒有截斷或 provider 429，但正文僅 374 漢字，且 C3/C4 引用其他變例著法、K1/K2 因果主線不合格；正式 validator 拒絕，沒有交付正文、沒有合格正文 SHA-256。安全報告在工作區上一層 `pr-fixed-case-super-2026-09-26.json`。報告原本把 `HarnessExplanationUnavailableError` 傳給 provider 診斷器，產生 unknown；這不是未知的 key 驗證失敗，實際 model calls 已完成，拒絕來自完整講解品質。未提交的本機 runner 已改為單獨輸出 Harness reason，避免下次誤用 provider 分類；未為此再刷 API。

另核對本機 OS 為 Windows 11 Home、EditionID Core、build 26200；Microsoft [Windows Sandbox 文件](https://learn.microsoft.com/en-us/windows/security/threat-protection/windows-sandbox/windows-sandbox-overview) 不支援 Home。只讀預檢報告 `windows-isolation-preflight-2026-09-26.json` 位於工作區根目錄。使用者已確認目前沒有可用隔離 Windows 環境；沒有操作系統升級、新增授權或安裝虛擬化軟體。隔離更新器實機門檻維持未完成。

## 2026-09-26：提示中的負面範例殘留修正

完整提示複核發現比較契約雖禁止同首選硬找缺點，正文配額仍叫「實戰步問題」，C2 causal 範例仍預填「對手實際利用」；近似等值或證據不足也會預填 initiative_loss / opponent_development。最小修正為中性的比較／合理應對用語，負面 category 範例僅限 evidence_backed_difference，其他狀態使用發展類範例。五段 id、400 漢字下限、引用與因果 validator 不變。

新增同首選範例測試先紅（143 通過、1 失敗），修改後 Harness 145/145，包含證據不足不預填負面 category、有證據差異仍允許比較的正反例。本轮 `npm.cmd test`、`npm.cmd run build`、`npm.cmd run security:audit`、`npm.cmd run test:acceptance:ai`（fixed=6、soak=358）、`npm.cmd run test:updater-sdk` 均 exit 0。詳細 log 在工作區上一層 `pr76-2026-09-26-neutral-prompt-{test,build,audit,ai-acceptance,updater-sdk}.log`。此修正針對品質契約殘留，不宣稱已解決 374 漢字或跨變例引用的真實失敗；修改前的真實結果不能代替修改後驗收。使用者再次明確限制只能用免費模型，沒有新增付費服務。

對修正 commit `3ad416713239efc39d0061cf4d4b2c60a55050ac` 重做一次免費 Super 正式案例，報告 `pr-fixed-case-neutral-prompt-2026-09-26.json`：35,917 ms、兩次呼叫、均 stop、completion 1,653 / 1,823、reasoning 0 / 0，仍為 quality_validation_failed。正文 384 漢字、跨變例著法與單一 PV 過度確定語氣均被拒絕。此次亦暴露剛修改的 neutral 範例把 K1/K2 都預填 piece_development，與既有不同後果類型檢查衝突；已改用既有的兩種非負面範例類型 central_control / piece_development，另加回歸。最終本機 `npm.cmd test`（Harness 146/146）、build、security:audit、test:acceptance:ai、test:updater-sdk 全部重跑 exit 0，詳細 log 同上。仍無完整合格真實正文，發布門檻不解除；不對同一設定追加 API 重試。
## 2026-09-26：免費 Ultra 參數與完整生成結果

使用者明確限定免費模型。正式同 key 切換服務成功選用 `nvidia/nemotron-3-ultra-550b-a55b:free`；官方精確 endpoint <https://openrouter.ai/api/v1/models/nvidia/nemotron-3-ultra-550b-a55b:free/endpoints> 的 prompt/completion 價格均為 0，支援 reasoning，但未列 response_format 或 structured_outputs。因此僅對該精確免費 Ultra 移除 wire 的 response_format；仍由 Harness 要求 JSON 並完整驗證，Super 及其他模型原策略保持。新增 provider 正反測試先紅後綠，沒有放寬 JSON、length 或品質拒絕規則。

先對已提交 `269c961a24b15f32e4faa9e6a69148643dda27d1` 執行一次 Super 正式 Pikafish＋Harness 固定案例：安全報告 `pr-fixed-case-neutral-distinct-2026-09-26.json`，34,349 ms、兩次呼叫、max_tokens 各 4,000、reasoning_disabled、均 stop、completion 1,641 / 2,351、reasoning 0 / 0。首輪可見正文 296 漢字；修補後仍有跨變例引用與 K1 因果問題，quality_validation_failed，未交付正文。296 是首輪長度，不是合格修補答案長度。

Ultra 在上述本機參數修正後執行一次正式固定案例，命令 `electron.cmd tools/acceptance/local-openrouter-fixed-case.cjs ..\\pr-fixed-case-ultra-2026-09-26.json`，sourceCommit 明列 `269c961-with-local-ultra-wire-fix`。真實 Pikafish depth 18、best g3g4、user h2e2；共 6,877 ms，initial_combined 首呼叫與既有一次有界重試均 HTTP 502 / provider_unavailable，各 max_tokens 4,000、reasoning max_tokens 1,000 / exclude true；finish_reason 與 token usage 未提供。兩次相同結果後停止，沒有完整正文，不冒充模擬或驗收通過。

透過正式 `OpenRouterSavedModelService` 恢復原本免費 Super 成功，重新開啟 SecretStore 安全 metadata 確認仍為 Super；同 key 安全額度計數 HTTP 200、used 7、limit 50、remaining 43。未使用付費模型、未輸出 key、未手改 AppData。此為 main-process 證據，不是候選 binary 設定頁驗收。

本機修正後 `npm.cmd test`、`npm.cmd run build`、`npm.cmd run security:audit`、`npm.cmd run test:acceptance:ai`、`npm.cmd run test:updater-sdk` 均 exit 0；log 位於工作區上一層 `pr76-2026-09-26-ultra-wire-{test,build,audit,ai-acceptance,updater-sdk}.log`。最終已提交提示修正 head 269c961 的 CI run 36212862788 SUCCESS；本次 provider 變更另需新 head CI。真實完整合格正文與隔離 Windows 更新器實機門檻仍未完成，不合併或發布。

## 2026-09-26：逐手棋盤事實提供給完整比較

重讀正式首輪與修補提示，確認只有中文主線、opponentReplies 與引擎摘要，未提供逐手可計算棋盤事實。新增 VariationBoardFacts，重用 parseFen、applyUciMove、formatChineseMove 與 isKingInCheck，從每個 evidence 自己的起始局面獨立計算最多 12 手的方別、移動棋子、移動路數、吃子及將軍。中文記譜不符或非法後續即停止，只保留合法前綴並明列 warning；這些事實不代表戰略判斷經引擎證明。首輪及同一次有界修補均使用，沒有放寬 validator、400 漢字下限、兩次呼叫或 token/time 上限。

正式提示缺少棋盤事實的回歸先紅（146 通過、1 失敗），實作及棋盘正反例補完後 Harness 152/152：方別／路數正例、實際吃子同時將軍、錯誤輪走方、跨變例中文記譜及非法後續停止。build、離線 AI acceptance、updater SDK exit 0；npm test 與 security audit 原執行 handle 在完成後已不存在，完整 log 分別結束於 E2E 94/94 與稽核通過，且沒有相關測試程序仍在執行。logs 為工作區上一層 pr76-board-facts-{red,harness,test,build,audit,ai-acceptance,updater-sdk}.log。本次新增事實是針對可重現的證據輸入缺項；是否改善真實正文仍需新的正式固定案例，不能用離線結果替代。

使用者已要求暫緩隔離 Windows／VM 建置，沒有安裝 VirtualBox、建立 VM、變更系統功能或修改正式 AppData；隔離更新器實機項目保持未驗收。

修正 commit a57bfc1114f37bf3707fc76d19ca7f0ec761f4ef 的一次正式免費 Super 固定案例報告：pr-fixed-case-board-facts-2026-09-26.json。Pikafish depth 19，首選 g3g4，實戰 h2e2，根分析已有完整 21 手實戰線。Harness 共 36,914 ms、兩次呼叫，max_tokens 各 4,000，reasoning_disabled；均 stop，completion 1,603 / 2,044、reasoning 0 / 0。仍為 quality_validation_failed：首輪正文 299 漢字，C3 混用其他變例的馬8進7；修補審核仍缺少兩步實際主線及具體位置／棋子關係。299 是首輪長度，沒有合格修補正文或 SHA-256。新增事實改善證據輸入的完整性，但這次結果沒有證明真實生成已修復；不對相同設定再刷 API。

獨立只讀審查 a57bfc1 未發現本次逐手棋盤事實的具體阻擋 bug：user PV 選取條件與 makeEvidence 一致，Pikafish 的 user PV 含首手；方別與合法性由 applyUciMove 驗證，captured 與 givesCheck 來自對應走後棋盤；無效手在 push 前停止，最多 12 手。這是 source review，不代替 provider 或 binary 驗收。另僅查詢公開官方 nex-agi/nex-n2.5-pro:free/endpoints，回傳 id 正確但 endpoints=[]；未執行正式切換或生成、未讀取不同 key 的 saved Nex 憑證，active Super 不變。沒有新增付費模型，也沒有以相同設定再刷 API。

## 2026-09-26：檢查先前修改是否真正解決問題

以 `ce03ba0...d038f1a` 固定 PR 範圍檢查實際契約；不是以先前 CI 成功推論問題已解決。獨立讀碼及正式 validator 離線重現找到以下缺口，均已補修正：

- 不足聲明的「需要／缺少」分支會丟掉整段實質斷言；另外只看 claim.text 可豁免另附 causal。改為只接受完整、有限的不足分句；未識別的剩餘斷言保留並檢查，有 causal 時不能單靠不足正文豁免。保留「目前引擎證據不足，無法確認」正常正例。
- 同首選的負面矛盾可以藏在 directAnswer；正式 validator 現在檢查完整內容中的明確實戰步負評，否定或條件敘述不當成已斷言的負評。正常同首選五段、負評反例及「不能說這步是失誤」正例均已跑過。
- 原本 VariationBoardFacts 只有 prompt 接線；合法 ID 與模型 verified=true 可讓「黑方炮二平五吃紅車」「紅方馬8進7將軍吃炮」通過。新增有界的明確著法斷言核對，audit、正文、directAnswer、causal 分別依其引用變例重播既有棋盤工具，檢查走子方、吃子對象與將軍；缺少可計算或有歧義的事實不能支持確定吃子／將軍。這不是通用自然語言或策略真偽引擎，最終進度不再宣稱所有棋盤事實／棋理解釋已獨立驗證。
- 初次完整提示一邊要求長文，一邊示範 C1 與單句 directAnswer 相同；改成先寫五段正文、再填精簡 audit，schema 明列每段正文目標。修補重用原始輸出契約與證據及錯誤，不把被拒絕的草稿斷言再送回模型。400 漢字、五段、正式 JSON validator、最多兩次呼叫及 token/time 總上限不變。
- Release 的 Get-FileHash 寫出大寫 SHA-256，promotion 使用 sha256sum 小寫並做逐字比較，導致合法候選也必定被拒絕。已只正規化 hash 大小寫，檔名仍逐字核對；以 workflow 實際 Bash 檢查段測試 uppercase+CRLF、lowercase、錯 hash／錯檔名。原 head 的正確大寫 manifest 實際 exit 1，修正後正例 pass、反例拒絕。

針對缺口的回歸：正文優先／不回灌草稿先紅 152/2；formal audit／answer 假吃子與將軍先紅 154/2；同首選直接結論負評先紅 156/3；修完 Harness 159/159。Variation statement 測試先紅 6/9、修完 15/15，並加入 npm pretest，移除測試中暫時缺 checker 的 fallback。security test 81/81。所有離線證據只證明程式行為，不代替真實 provider 或候選 Windows binary。

本輪必要檢查的 logs 位於工作區上一層 `pr76-effective-review-{test,build,audit,ai-acceptance,updater-sdk}.log`；其實際 exit 結果與修改後真實固定案例在後續段落記錄。免費模型限制不變，未操作使用者取消的隔離環境建置。

本輪五個必要入口均取得實際 exit 0：`npm.cmd test`（Harness 159/159、quality 59/59、security 81/81、Variation statements 15/15、E2E 94/94）、`npm.cmd run build`、`npm.cmd run security:audit`、`npm.cmd run test:acceptance:ai`（本輪 fixed=6、soak=358）及 `npm.cmd run test:updater-sdk`。測試 session 54288、build 23464、audit 68775 均由原 handle 讀到完成與 exit 0；沒有因等待而重啟。此證據仍是 source／離線分類。

修正 commit `68da9016d9ba7855fa30ae96bba960668fd5038a` 已推到 PR #76，正式固定案例報告 `pr-fixed-case-effective-review-2026-09-26.json`：相同已保存 key／active 免費 Super，免費 catalog eligible=true；Pikafish depth 18，首選 g3g4、實戰 h2e2，Harness 50,927 ms（本次根快照實戰線只有首手，Harness 先取得較深實戰證據）。initial_combined／repair 兩次回覆均 stop，completion 2,695／2,547，reasoning 0／0，各 max_tokens 4,000，耗時 24,716／20,769 ms。正文這次沒有回報低於 400 漢字，但後果審查、C4 跨變例引用與 C5 缺引用仍未通過。沒有合格最終正文、實際正文長度或 SHA-256，不將缺少篇幅錯誤冒充完整生成通過。

另由該診斷複核新棋盤檢查的歧義處理：同名著法可能在一條線重複出現，吃子不同不應連帶抹去所有步數一致的將軍事實。補離線正例先紅 15/1，改成對走子方／吃子／將軍各自核對相關事實，修完 16/16；吃子結果真正不同時仍拒絕未指明步數的確定吃子斷言。此修正不變更送給 provider 的設定或 prompt，不為相同設定再刷 API，也不能使前述失敗變成成功。

歧義修正後五個必要入口重新取得 exit 0，log 為工作區上一層 `pr76-final-effective-{test,build,audit,ai-acceptance,updater-sdk}.log`；full test session 98159、build 93090 均讀取原 handle 完成。`68da901` 的 GitHub Windows CI run `36244245346` 已 SUCCESS；後續程式 head 仍須由新 CI 覆蓋。只用正式 saved-model service 讀取免費 catalog，active Super 未變；catalog 17 個可選免費 ID，不等於每個 provider 已經通過完整正文驗收。没有修改正式更新來源、AppData 或已安裝 binary。

## 2026-09-26：減少合併 JSON 的重複後果欄位

`d04c645fc866754f960eadf8f80f8f6e153727c3` 已推送，CI run `36244498133` SUCCESS。針對前次真實回覆已沒有篇幅錯誤、audit 卻仍漏主線／棋子關係的結果，合併初答及一次修補使用精簡 audit：K1/K2 以 claimId 引用對手後果段的 C4a/C4b，不再要求模型把同一後果重新寫成 summary、opponentUse、boardImpact。程式只複製該模型 claim 的原文及 causal 內容，並從其引用變例逐字辨認 supportingMoves；不生成補字、戰術或因果，不修復截斷 JSON。完整 JSON 解析及同一正式 audit／answer／scorer 仍全部執行。保留舊完整 audit 格式相容性；這只減少重複內部輸出。

claimId 必須唯一存在於後果段，claim.findingIds 要連到同一 K，且只引用實戰線；同首選時，真正以該首手開始的相同首選線也可使用。空引用、錯 ID、重複 claim、另一變例或缺少 causal 都不補造內容，仍拒絕交付。400 漢字／五段、兩次呼叫、每次 4,000 及共享總 token/time 上限均未調整。

正式 Harness 新正例先紅 159/1；補完 compact normalization 與四個反例後 164/164，typecheck:node 通過。相關本機 log 為 `pr76-compact-audit-{red,harness,typecheck,test,build,audit,ai-acceptance,updater-sdk}.log`；後續最新完整 test/build log 為 `pr76-compact-final-{test,build}.log`。本輪 audit、離線 AI acceptance（fixed=6、soak=358）、updater-sdk 已 exit 0；最後程式狀態的 test/build 與真實 fixed case 在下方續記，未取得合格正文前不合併／發布。

最新完整 `npm.cmd test`（164/164 Harness、59/59 quality、16/16 variation statements、81/81 security、94/94 E2E）與 `npm.cmd run build` 已由原 session 21281／50085 讀到完成及 exit 0。沒有重啟原命令。五個必要入口皆通過，仍只屬離線程式驗證。

精簡 audit commit `9eb1a6801212ea41017206ecab37586574f579c3` 的 CI run `36244820117` SUCCESS。一次正式免費 Super 固定案例：`electron.cmd tools/acceptance/local-openrouter-fixed-case.cjs ..\pr-fixed-case-compact-audit-2026-09-26.json`（source commit 環境變數為該完整 SHA）。Pikafish depth 19，首選 b2e2、實戰 h2e2，根分析分別有 25／17 手主線。Harness 56,340 ms、兩次 initial_combined 呼叫：首呼叫 16,694 ms provider_unavailable，未提供 HTTP status、finish reason 或 token usage，不據此稱實際 502／503；一次有界重試 38,999 ms 回 stop、completion 2,588、reasoning 0。各請求 max_tokens 4,000、reasoning_disabled。沒有篇幅或 audit 主線錯誤，但 K2／C4b 把紅方「馬八進九」寫成對手著法，C5 提到炮二平五卻只引用首選線；正式 validator 拒絕，仍沒有合格正文／SHA-256。

由此複核發現 C5 schema 範例確實預填只有首選 evidence，與比較兩種著法時的引用規則衝突。只把 C5 範例改為同時引用首選／實戰線，並明列 computedBoardFacts 的 red=紅方、black=黑方及 opponentUse 必須使用 opponentReplies，未刪除方別或跨變例檢查。C5 regression 先紅（164 通過、1 失敗），修完 Harness 165/165。最新五個必要入口均 exit 0，logs 在上一層 `pr76-principle-scope-{test,build,audit,ai-acceptance,updater-sdk}.log`；test 原 session 62438、build 51901 讀到完成，E2E 94/94，離線 AI fixed=6／soak=358。修正後另做一次正式完整案例；前述失敗不改成通過，不對相同 prompt 再刷 API。

修正 commit `6e320e41c9a107fe25b9b5d5db7d89a3bab7d52b` 的 CI run `36245083377` SUCCESS；一次正式固定案例報告 `pr-fixed-case-principle-scope-2026-09-26.json`。仍為同 saved key／免費 Super，catalog eligible=true；Pikafish depth 19，首選 b2e2／實戰 h2e2，根主線 25／21 手。Harness 41,331 ms，initial_combined／repair 各 4,000 token 上限，reasoning_disabled，兩次均 stop；completion 2,216／2,384、reasoning 0／0、耗時 20,299／20,983 ms。沒有 generation_incomplete；但首輪 K1 主線不足、跨變例引用／錯誤應手，修補後 C5 缺引用及必然應對檢查仍失敗。沒有合格最終正文、正文長度或 SHA-256；不能把截斷未再現說成整體回覆問題已解決。

持續檢查實際效果，又以正式 Harness 離線重現：短追問的 plain-text salvage／recovery 僅檢查相關性與出現主線著法，會接受「黑方炮二平五吃紅方車」；修正為兩個入口都使用既有逐手棋盤檢查，錯誤回覆仍失敗、正常短文可交付且不強迫五段。另原本必然論 regex 誤擋「不是唯一回應／不能說黑方被迫」，卻漏掉隱藏 causal 裡的肯定必然論；改成逐分句、逐斷言保留局部否定，不能把否定重用至同段下一個肯定斷言，並包含 causal。正反例先紅 167/7；修正及補 salvage／相鄰斷言回歸後 Harness 178/178。原正常 fixture 的 causal 曾含「被迫」，改成明確只描述該條主線的走法；否定與肯定反例分別保留，不以刪除檢查換通過。

writer／區塊 repair 提示仍稱「已驗證具體後果」，研究進度也稱已確認；現在改為通過結構與引用檢查的模型後果摘要，明確說 verified 不是引擎證明。最新 `npm.cmd test`、build、security:audit、test:acceptance:ai、test:updater-sdk 全部取得 exit 0，logs 為 `pr76-recovery-assertion-{red,harness,test,build,audit,ai-acceptance,updater-sdk}.log`；原 test session 39708、build 9962 完成。這些局部驗證修正不改初次完整生成 prompt 或 provider 設定，不對相同設定盲目重跑真實生成。完整合格正文仍是未完成門檻；不合併或發布。
